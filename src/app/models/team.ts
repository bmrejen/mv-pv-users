export class Team {
  constructor(
              public type: string = "users",
              public id: string,
              public name: string,
              public description: string,
              ) {
    //
  }
}
